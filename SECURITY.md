# 安全与脱敏

本仓库只记录可公开复用的部署方法，不应提交以下内容：

- API Key、Token、Cookie、SSH 私钥与任何认证头；
- 公网 IP、VPN 地址、内网拓扑地址、真实主机名；
- 企业名称、人员姓名、工单号和内部域名；
- 原始日志、容器完整环境变量、业务请求正文；
- 带用户名的本地路径、未脱敏截图与监控面板地址。

示例中的路径、网卡、端口和密钥均为占位符。实际部署时建议把秘密放入未被版本控制的 `.env` 或专用密钥管理系统，不要直接写入 Compose 文件。

如果凭据曾出现在终端、聊天记录或截图中，应立即轮换，不要只依靠“删除记录”。

